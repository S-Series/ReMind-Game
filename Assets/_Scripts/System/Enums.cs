namespace Enums
{
    public enum GameMode { Normal, Mirror, Random, SRandom, Error }
    public enum LineMode { Line4, Line5 }
    public enum GuageMode { Easy, Normal, Hard }

    public enum NoteType { Normal, Scratch, Airial, Speed, Effect }
    public enum JudgeType { SPure, Pure, Rough, Lost }
}   